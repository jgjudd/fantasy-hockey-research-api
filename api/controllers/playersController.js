'use strict';

exports.get_player_bio = (req, res) => {
  // Book.find({}, (err, book) => {
  //   if (err)
  //     res.send(err);
  //   res.json(book);
  // });
  res.json({ "test": "message" });
};

exports.get_player_stats = (req, res) => {
  res.json({ "test": "message" });
};